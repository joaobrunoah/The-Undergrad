import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  SafeAreaView: {
    zIndex: 1,
    position: "absolute"
  },
  foto: {
    zIndex: 0,
    width: "100%",
    height: "100%"
  }
});

export default styles;
